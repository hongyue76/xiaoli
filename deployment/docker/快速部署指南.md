# 🚀 快速部署指南（5分钟完成）

## 前提条件

- [x] 腾讯云学生机（已购买）
- [x] 本地电脑（用于上传文件）

---

## 第一步：连接服务器

```bash
ssh root@你的服务器IP
```

---

## 第二步：上传部署文件

**方法A：Git 克隆（推荐）**
```bash
ssh root@你的服务器IP
git clone https://github.com/hongyue76/xiaoli.git
cd xiaoli/deployment/docker
```

**方法B：本地打包上传**
```bash
# 本地执行
cd d:/me/project/xiaoli
scp -r deployment/docker root@你的服务器IP:/root/
```

---

## 第三步：配置 API 密钥

```bash
cd /root/docker

# 创建配置文件
cat > .env << 'EOF'
DB_PASSWORD=Xiaoli@2024
DEEPSEEK_API_KEY=sk-your-deepseek-key
DELILEGAL_APP_ID=your-delilegal-app-id
DELILEGAL_SECRET=your-delilegal-secret
EOF
```

---

## 第四步：一键部署

```bash
# 给脚本添加执行权限
chmod +x deploy.sh

# 运行部署脚本
bash deploy.sh
```

或者手动部署：
```bash
docker-compose -f docker-compose.simple.yml up -d --build
```

---

## 第五步：验证

```bash
# 查看服务状态
docker-compose -f docker-compose.simple.yml ps

# 测试访问
curl http://localhost

# 查看日志
docker-compose -f docker-compose.simple.yml logs -f
```

---

## 访问应用

```
浏览器打开: http://你的服务器IP
```

---

## 常用命令

| 命令 | 说明 |
|------|------|
| `docker-compose ps` | 查看状态 |
| `docker-compose logs -f` | 查看日志 |
| `docker-compose restart` | 重启服务 |
| `docker-compose down` | 停止服务 |
| `docker-compose up -d --build` | 重新构建启动 |

---

## 需要帮助？

详细文档：`DEPLOY_TENCENT_CLOUD.md`
